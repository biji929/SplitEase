<?xml version="1.0" encoding="utf-8"?>
<!--
    Reusable menu-row card for ProfileActivity.

    Usage — set these in ProfileActivity.kt after inflate:
        row.findViewById<ImageView>(R.id.ivMenuIcon).setImageResource(...)
        row.findViewById<TextView>(R.id.tvMenuLabel).text = "..."
        row.setOnClickListener { ... }

    Or bind icon/label data inside the include tag via data-binding if you
    switch to ViewBinding / DataBinding later.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="14dp"
        android:paddingVertical="13dp">

        <!-- Icon container (rounded square) -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardMenuIconBg"
            android:layout_width="36dp"
            android:layout_height="36dp"
            app:cardCornerRadius="10dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/menu_icon_bg">

            <ImageView
                android:id="@+id/ivMenuIcon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                android:contentDescription="@null" />
        </androidx.cardview.widget.CardView>

        <!-- Label -->
        <TextView
            android:id="@+id/tvMenuLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:textStyle="normal" />

        <!-- Chevron -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="›"
            android:textColor="@color/text_hint"
            android:textSize="18sp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>